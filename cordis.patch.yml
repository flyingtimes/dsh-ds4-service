# The dsh bundle patch shipped inside this package. Declared via
# `dsh.bundle.patch` in package.json, this file becomes an active profile
# layer automatically after the package is added to the profile (pnpm link +
# listed in package.json's dsh.profile.bundles) — no manual editing needed.
- insert:
    - id: ds4-service
      name: 'dsh-ds4-service'
