{
  "serviceDir": "~/code/ds4-on-mac",
  "model": "{{assets}}/DeepSeek-V4-Flash-0731-Abliterated-DS4-Quality128.gguf",
  "port": 8000,
  "ctx": 393216,
  "threads": 20,
  "kvDir": "~/.ds4/server-kv",
  "kvSpaceMb": 65536,
  "mtp": "{{assets}}/DeepSeek-V4-Flash-0731-Abliterated-DS4-Quality128-DSpark-support.gguf",
  "dspark": false,
  "warm": true,
  "deployAssets": true,
  "logLines": 30,
  "statusPollMs": 5000,
  "_comment": {
    "serviceDir": "服务运行目录（工作目录）。默认 ~/code/ds4-on-mac，即你的 ds4 项目根；日志/pid 都写在这里的 logs/。改成空目录时插件会把自带的二进制和启动脚本部署过去，实现完全自包含。",
    "model": "模型文件：相对 serviceDir、绝对路径，或 {{assets}} 占位符（=插件 assets 目录，用 assets/download.sh 下载后即指向它）",
    "port": "监听端口。",
    "ctx": "上下文长度（-c）。0731 的 reasoning_effort=max 需要 393216+，低于会自动回退 high。",
    "threads": "主机辅助线程数（-t）。",
    "kvDir": "KV 持久化磁盘目录（--kv-disk-dir），默认 $HOME/.ds4/server-kv。",
    "kvSpaceMb": "KV 磁盘空间上限 MB（--kv-disk-space-mb），默认 65536。",
    "mtp": "DSpark 配套模型（--mtp）：支持 {{assets}} 占位符；留空禁用。用 assets/download.sh --dspark 下载",
    "dspark": "是否启用 DSpark 投机解码（--dspark）。M2 Ultra 上实测比普通解码慢，默认关。",
    "warm": "是否预触全部映射页（--warm-weights），避免首请求吃缺页。启动会慢约同样时长。",
    "deployAssets": "若 serviceDir 缺少 start.sh 或 ds4-server，是否把插件 assets/ 里自带的复制过去。默认 true。",
    "logLines": "日志面板默认读取的行数。",
    "statusPollMs": "GUI 状态轮询间隔毫秒。"
  }
}
